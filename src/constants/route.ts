// 公共路由path
export const ROUTE_PATH_COMMON = ['/login', '/404']

// 路由路径
export const ROUTE_PATH = {
  LOGIN: '/login',
  Notification: '/404',
}

// 路由参数名称
export const ROUTE_PARAM_NAME = {
  REDIRECT_INFO: 'redirectInfo',
}

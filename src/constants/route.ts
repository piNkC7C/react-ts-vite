// 公共路由path
export const ROUTE_PATH_COMMON = ['/login', '/404']
// 最顶层key
export const ROUTE_KEY_TOP = 'top'

// 路由路径
export const ROUTE_PATH = {
  LOGIN: '/login',
  NOT_FOUND: '/404',
  AUTH: '/',
}

// 路由key
export const ROUTE_KEY = {
  LOGIN: 'login',
  NOT_FOUND: 'notFound',
  AUTH: 'auth',
}

// 路由参数名称
export const ROUTE_PARAM_NAME = {
  REDIRECT_INFO: 'redirectInfo',
}

// 组件路径
export const ROUTE_ELEMENT_PATH = {
  LOGIN: '/Login',
  NOT_FOUND: '/NotFound',
}
